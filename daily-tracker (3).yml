name: Daily Winbond Price Tracker

on:
  schedule:
    # Runs every day at 4:00 AM UTC = 12:00 PM Taipei time (UTC+8).
    # Note: GitHub Actions scheduled runs are best-effort and can be
    # delayed, especially on low-activity repos.
    - cron: "0 4 * * *"
  workflow_dispatch: {}  # lets you also trigger it manually from the Actions tab

jobs:
  track-prices:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install requests

      - name: Run price tracker
        env:
          DIGIKEY_CLIENT_ID: ${{ secrets.DIGIKEY_CLIENT_ID }}
          DIGIKEY_CLIENT_SECRET: ${{ secrets.DIGIKEY_CLIENT_SECRET }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: python track_prices.py

      - name: Commit updated prices.csv back to the repo
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add prices.csv
          git diff --staged --quiet || git commit -m "Daily price update $(date -u +'%Y-%m-%d')"
          git push
